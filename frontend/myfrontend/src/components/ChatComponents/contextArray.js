import React, { useEffect, useState, useRef,useContext } from "react";
import {SocketContext} from '../../context/SocketContext';

export const setRecipientArray = () =>{
    //const clientSocket = useContext(SocketContext);
}